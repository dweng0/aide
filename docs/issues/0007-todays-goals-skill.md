# #7 — Today's Goals Skill

**Labels:** enhancement · ready-for-agent

## What to build

Write `skills/todays-goals.json` for use in opencode and Claude Code. This is the morning ritual skill — it converts a loose statement of intent into a structured, measurable plan for the day.

Skill flow:
1. Ask "what do you want to achieve today?"
2. For each goal stated, ask follow-up questions to sharpen it into concrete subtasks (what does done look like? any dependencies? any blockers?)
3. Write results to `daily-goals.md` — goal name as `##` heading, sharpened subtasks as `- [ ]` checkboxes

Future: will also read the user's calendar to surface today's meetings and attendees before the drill-down (see `roadmap.md`).

## Acceptance criteria

- [ ] Skill is triggerable from opencode and Claude Code
- [ ] Skill asks at least one follow-up question per goal to sharpen subtasks
- [ ] Results are written to `daily-goals.md` in the correct format
- [ ] Running the skill a second time on the same day appends to (or replaces) the file cleanly — no duplicate headings
- [ ] Skill handles multiple goals in a single session

## Blocked by

None — can start immediately (#1 scaffold is done).

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Write the Today's Goals Skill — the morning ritual that converts loose intent into a measurable daily plan.

**Current behavior:**
No Today's Goals Skill exists. `daily-goals.md` is empty on a fresh install.

**Desired behavior:**
A skill JSON file (compatible with opencode and Claude Code) that runs a mini drill-down session each morning:
1. Asks "what do you want to achieve today?"
2. For each goal the user states, asks at least one follow-up question to sharpen it into concrete subtasks — what does done look like? any blockers?
3. Writes the results to `daily-goals.md` as `##` goal headings with `- [ ]` subtask checkboxes.

If the user runs the skill a second time on the same day, it appends new goals rather than overwriting — no duplicate `##` headings.

**Key interfaces:**
- Skill JSON shape: `{ name, description, triggers, instructions }`
- `daily-goals.md` output format:
  ```
  ## Goal name
  - [ ] Subtask one
  - [ ] Subtask two
  ```
- Skill instructions must reference the `aide` repo path for `daily-goals.md` dynamically

**Acceptance criteria:**
- [ ] Skill is triggerable by name from opencode and Claude Code
- [ ] Skill asks at least one follow-up question per stated goal
- [ ] Results are written to `daily-goals.md` in the correct format
- [ ] Running the skill twice on the same day appends without duplicating headings
- [ ] Skill handles multiple goals in a single session

**Out of scope:**
- Reading the user's calendar (future — see `roadmap.md`)
- Checking goals against the roadmap (future — Roadmap Skill)
