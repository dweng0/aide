# #3 — Cleanup script

**Labels:** enhancement · done

## What to build

Write `scripts/cleanup_planner.py`. When called, it reads `user-planner.md`, removes any line where the checkbox is checked (`[x]`), and rewrites the file in place. This is called automatically by skills at the end of every Skill Session.

## Acceptance criteria

- [x] `cleanup_planner.py` exists in `aide/scripts/`
- [x] Running the script removes all `- [x]` lines from `user-planner.md`
- [x] Unchecked `- [ ]` lines are preserved exactly
- [x] The script is a no-op if `user-planner.md` is empty or has no checked items
- [x] The script resolves the path to `user-planner.md` relative to the `aide` repo (not hardcoded)

## Blocked by

None — can start immediately (#1 scaffold is done).

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Write a Python script that strips resolved items from the User Planner.

**Current behavior:**
`user-planner.md` does not exist yet. No cleanup mechanism exists.

**Desired behavior:**
`cleanup_planner.py` reads `user-planner.md`, removes every line where the checkbox is checked (`- [x]`), and rewrites the file in place. Unchecked lines (`- [ ]`) and any non-checkbox lines (headings, blank lines) are preserved exactly. The script resolves the path to `user-planner.md` relative to the `aide` repo root so it works regardless of where it is called from.

**Key interfaces:**
- `user-planner.md` checkbox format: `- [x] description — see ~/notes/DD-MM-YY.md` (checked) and `- [ ] description — see ~/notes/DD-MM-YY.md` (unchecked)
- Script must be callable with no arguments — path resolution is internal

**Acceptance criteria:**
- [x] Script is present in `aide/scripts/` and executable
- [x] All `- [x]` lines are removed after running
- [x] All `- [ ]` lines are preserved exactly
- [x] Non-checkbox content (headings, blank lines) is preserved
- [x] Script is a no-op if the file is empty or has no checked items
- [x] Script does not error if `user-planner.md` does not exist

**Out of scope:**
- Archiving checked items before removing them
- Cleaning `daily-goals.md` (separate concern)
