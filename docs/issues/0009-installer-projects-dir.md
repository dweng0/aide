# #9 — installer: projects/ directory

**Labels:** enhancement · done

## What to build

Update `install.sh` to create `~/.notes-context/projects/` on a fresh install, so the parent directory exists before any `project-start` session writes into it. Also verify the README "what the installer sets up" table covers this new directory.

Changes are small and localised:
- Add `mkdir -p "$CONTEXT_DIR/projects"` alongside the existing `mkdir -p "$CONTEXT_DIR"` call
- Add a row for `Projects dir` to the README table if it is not already there

## Acceptance criteria

- [ ] Running `install.sh` on a fresh machine creates `~/.notes-context/projects/`
- [ ] Running it a second time does not error (idempotent)
- [ ] README documents `~/.notes-context/projects/` in the "what the installer sets up" table

## Blocked by

None — can start immediately (parallel with #8).

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Update `install.sh` to pre-create `~/.notes-context/projects/` and add it to the README installer table.

**Current behavior:**
`install.sh` creates `~/.notes-context/` and the six context files, but does not create `~/.notes-context/projects/`. On a fresh install, running `/project-start` must create this directory itself, and the README does not document the `projects/` directory at all.

**Desired behavior:**
`install.sh` adds a single `mkdir -p "$CONTEXT_DIR/projects"` call so the parent directory exists from the start. The README "what the installer sets up" table gains a row for the projects directory.

**Key interfaces:**
- `install.sh` structure: uses `$CONTEXT_DIR` variable already defined — add `mkdir -p "$CONTEXT_DIR/projects"` alongside or after the existing `mkdir -p "$CONTEXT_DIR"` line
- README table format: markdown pipe table under "What the installer sets up" — add a `Projects dir` row with path `~/.notes-context/projects/` and purpose "Per-project context and stakeholder files"

**Acceptance criteria:**
- [ ] Running `install.sh` on a fresh machine creates `~/.notes-context/projects/`
- [ ] Running it a second time does not error (idempotent — `mkdir -p` guarantees this)
- [ ] README documents `~/.notes-context/projects/` in the installer table

**Out of scope:**
- Creating any subdirectory under `projects/` (that is `project-start`'s job)
- Changes to `.gitignore` (already covers `.notes-context/` entirely)
