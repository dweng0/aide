# #8 — project-start skill

**Labels:** enhancement · done

## What to build

Write the `project-start` skill (`skills/project-start.json` + `skills/project-start/SKILL.md`). This is a one-time setup skill run at the beginning of a new project. It conducts a structured interview, then writes two files into `~/.notes-context/projects/{name}/`:

- `project-context.md` — project name, end goal, MVP definition, timelines
- `stakeholders.md` — list of people with name, role, and email

The skill creates the `~/.notes-context/projects/{name}/` directory on first run if it does not exist.

**Interview flow:**
1. Infer the project name from `CONTEXT.md` (name field) in the current directory, or the directory name, or ask if neither resolves.
2. Ask: end goal ("done when..."), MVP definition, key milestones/timelines.
3. Ask for stakeholders one at a time (name, role, email). Loop until the user says they're done.
4. Before writing, show a summary and ask for confirmation.
5. Write both files.

**Re-run behaviour:** If `project-context.md` already exists for this project, warn the user ("This project already has context from a previous run") and offer to update it. On confirmation, re-interview and overwrite both files.

## Acceptance criteria

- [ ] `skills/project-start.json` exists with `name`, `description`, `triggers`, and `instructions` keys
- [ ] Skill is triggerable by `/project-start` in Claude Code and opencode
- [ ] Skill infers the project name from `CONTEXT.md` or cwd; asks if it cannot resolve
- [ ] Interview covers end goal, MVP, timelines, and stakeholders (name/role/email)
- [ ] `project-context.md` is written to `~/.notes-context/projects/{name}/` with all interview fields
- [ ] `stakeholders.md` is written with one entry per stakeholder (name, role, email)
- [ ] Skill creates `~/.notes-context/projects/{name}/` if it does not exist
- [ ] Re-running warns the user and offers to update; overwrites both files on confirmation

## Blocked by

None — can start immediately.

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Write the `project-start` skill — a one-time setup interview that captures project context and stakeholders and writes them to `~/.notes-context/projects/{name}/`.

**Current behavior:**
No `project-start` skill exists. The `roadmap-gps` skill requires it to have been run first but has nothing to guard against it being missing beyond a runtime message. There is no structured way to capture project name, end goal, MVP, timelines, or stakeholders.

**Desired behavior:**
A skill (JSON entry point + SKILL.md instructions) that runs a structured interview when triggered with `/project-start`:
1. Infers the project name from the `name` field in `CONTEXT.md` in the current directory, falls back to the directory name, or asks if neither resolves.
2. Asks for: end goal ("done when..."), MVP definition, key milestones and timelines.
3. Asks for stakeholders one at a time (name, role, email), looping until the user signals done.
4. Shows a summary and asks for confirmation before writing.
5. Writes `project-context.md` and `stakeholders.md` into `~/.notes-context/projects/{name}/`, creating the directory if needed.

Re-run: if `project-context.md` already exists, warn the user and offer to update. On confirmation, re-interview and overwrite both files.

**Key interfaces:**
- Skill JSON shape: `{ name, description, triggers, instructions }` — same structure as `roadmap-gps.json` and `todays-goals.json`
- SKILL.md frontmatter: `name` and `description` fields (required by bats tests)
- `project-context.md` fields: project name, end goal, MVP, timelines — use markdown headings
- `stakeholders.md` format: one entry per person with name, role, and email; use a consistent list or table structure
- Output path: `~/.notes-context/projects/{name}/` — the `{name}` segment must match what `roadmap-gps` will look for

**Acceptance criteria:**
- [ ] `skills/project-start.json` is valid JSON with `name`, `description`, `triggers`, and `instructions` keys
- [ ] Skill is triggerable by `/project-start` in Claude Code and opencode
- [ ] Skill infers project name from `CONTEXT.md` name field → directory name → ask
- [ ] Interview covers end goal, MVP, timelines, and stakeholders (name/role/email)
- [ ] `project-context.md` is written to `~/.notes-context/projects/{name}/` with all interview fields
- [ ] `stakeholders.md` is written with one entry per stakeholder (name, role, email)
- [ ] Skill creates `~/.notes-context/projects/{name}/` directory if it does not exist
- [ ] Re-running warns the user and offers to update; overwrites both files on confirmation

**Out of scope:**
- Integrating with the Roadmap GPS skill beyond producing the files it expects
- Calendar or email lookup for stakeholder discovery
- Updating `CONTEXT.md` in the repo (read-only for this skill)
