# #10 — bats tests for project-start skill

**Labels:** enhancement · done

## Parent

#8 — project-start skill

## What to build

Write `tests/project_start_skill.bats` following the same pattern as `tests/todays_goals_skill.bats`. These are structural tests — they validate that the skill files exist and contain the expected content, not that Claude produces correct output at runtime.

Tests to cover:
- `project-start.json` is valid JSON with all required keys (`name`, `description`, `triggers`, `instructions`)
- `skills/project-start/SKILL.md` references both output files (`project-context.md` and `stakeholders.md`)
- SKILL.md documents the re-run/update behaviour (warn + offer)
- SKILL.md covers all interview fields (end goal, MVP, timelines, stakeholders)
- SKILL.md specifies the `~/.notes-context/projects/{name}/` output path

## Acceptance criteria

- [ ] `tests/project_start_skill.bats` exists and is runnable via `bats tests/project_start_skill.bats`
- [ ] All tests pass against the skill files produced by #8
- [ ] Tests follow the same structure and style as `tests/todays_goals_skill.bats`

## Blocked by

- #8 — project-start skill must exist before tests can pass

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Write `tests/project_start_skill.bats` — structural bats tests that verify the project-start skill files have the correct shape and content.

**Current behavior:**
No tests exist for `project-start`. The skill files from #8 will be present but unverified by the test suite.

**Desired behavior:**
A bats test file that mirrors the structure of `tests/todays_goals_skill.bats`. Tests are structural — they grep the skill files for required content rather than running a live skill session. All tests should pass once #8 is complete.

**Key interfaces:**
- Bats test style: use `@test "description" { ... }` blocks; assert with `grep -q` or `python3 -c` for JSON validation — match the existing test files exactly
- Skill JSON: validated with `python3` checking for `name`, `description`, `triggers`, `instructions` keys
- SKILL.md assertions: grep for `project-context.md`, `stakeholders.md`, re-run/warn language, interview field terms (end goal, MVP, timelines, stakeholders), and the `~/.notes-context/projects/` output path

**Acceptance criteria:**
- [ ] `tests/project_start_skill.bats` exists and passes `bats tests/project_start_skill.bats`
- [ ] Test verifies `project-start.json` is valid JSON with all four required keys
- [ ] Test verifies SKILL.md references both `project-context.md` and `stakeholders.md`
- [ ] Test verifies SKILL.md documents re-run/update behaviour
- [ ] Test verifies SKILL.md covers all interview fields (end goal, MVP, timelines, stakeholders)
- [ ] Test verifies SKILL.md specifies the `~/.notes-context/projects/` output path
- [ ] File structure and style match `tests/todays_goals_skill.bats`

**Out of scope:**
- Integration tests that actually invoke the skill at runtime
- Testing `install.sh` changes from #9 (covered by `tests/install.bats`)
