# #5 — Retrospective Skill

**Labels:** enhancement · ready-for-agent

## What to build

Write `skills/retrospective.json` for use in opencode and Claude Code. This skill is the "looking back" session — it surfaces open Soft Tasks from the User Planner and finds new ones hiding in recent Notes.

Skill flow:
1. Read `user-planner.md` and ask the user about each open item — has it been done?
2. Scan the last 14 days of Notes in `~/notes/` and identify new Soft Tasks (action items in prose not yet in the planner)
3. Add any new Soft Tasks to `user-planner.md` with a reference to the source Note file
4. Call `scripts/cleanup_planner.py` at the end of the session to strip resolved items

User Planner entry format: `- [ ] <natural language description> — see ~/notes/<dated-file>.md`

## Acceptance criteria

- [ ] Skill is triggerable from opencode and Claude Code
- [ ] Skill reads and surfaces all open items in `user-planner.md` before scanning notes
- [ ] Skill correctly identifies Soft Tasks from note prose (not just explicit checkboxes)
- [ ] New Soft Tasks are written to `user-planner.md` with source file references
- [ ] `cleanup_planner.py` is called at the end of every session
- [ ] Skill does not duplicate planner items already present

## Blocked by

- #3 (cleanup script must exist before skill calls it)

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Write the Retrospective Skill — the "looking back" session that surfaces and tracks Soft Tasks.

**Current behavior:**
No retrospective skill exists. The legacy `lifeline-intelligence.json` skill is read-only and passive.

**Desired behavior:**
A skill JSON file (compatible with opencode and Claude Code) that runs a structured session:
1. Reads `user-planner.md` and asks the user about each open `- [ ]` item one by one — has it been done? If yes, checks it off. If no, leaves it.
2. Scans `~/notes/` for the last 14 days of dated note files and reads their content, identifying Soft Tasks: action items mentioned in prose that are not already in `user-planner.md`.
3. For each new Soft Task found, appends a `- [ ]` entry to `user-planner.md` with a reference to the source note file.
4. At the end of the session, calls `cleanup_planner.py` to strip resolved items.

**Key interfaces:**
- Skill JSON shape: `{ name, description, triggers, instructions }` — follow the same structure as existing skills in `aide/skills/`
- `user-planner.md` entry format: `- [ ] <natural language description> — see ~/notes/DD-MM-YY.md`
- Skill instructions must reference the `aide` repo path dynamically (not hardcoded)

**Acceptance criteria:**
- [ ] Skill is triggerable by name from opencode and Claude Code
- [ ] Skill surfaces all open `user-planner.md` items and asks about each
- [ ] Skill correctly identifies Soft Tasks from note prose (not just explicit `- [ ]` checkboxes)
- [ ] New Soft Tasks are appended to `user-planner.md` with source file references
- [ ] No duplicate entries are added if the Soft Task is already in the planner
- [ ] `cleanup_planner.py` is called at end of every session

**Out of scope:**
- Modifying `daily-goals.md` (Introspective Skill's concern)
- Acting on Soft Tasks (scheduling, emailing) — LLM Planner feature, not in scope
