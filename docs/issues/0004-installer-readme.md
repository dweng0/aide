# #4 — Installer + README

**Labels:** enhancement · done

## What to build

Write `install.sh` and `README.md`. The installer is the single command a user runs on a fresh machine to get `aide` fully operational. It must be idempotent — safe to run multiple times.

`install.sh` does:
1. Creates `~/notes/` if it doesn't exist
2. Initialises each `.notes-context/` file (manifest.md, history_index.md, user-planner.md, daily-goals.md, goals-archive.md) if they don't already exist
3. Writes `~/.config/fish/conf.d/aide.fish` to add `aide/scripts/` to `$PATH`

`README.md` documents:
- What `aide` is
- Install steps: clone the repo, run `./install.sh`, open a new terminal
- What the installer sets up

## Acceptance criteria

- [x] `install.sh` is executable and idempotent
- [x] Running it on a fresh machine creates `~/notes/`, all `.notes-context/` files, and the fish PATH file
- [x] Running it a second time does not overwrite existing `.notes-context/` files
- [x] `README.md` covers what aide is, install steps, and what the installer does
- [x] A new terminal session after install can run `note_organizer.sh` by name (it's on PATH)

## Blocked by

- #2 (scripts must exist before installer wires them up)

---

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Write an idempotent installer and README that gets `aide` fully operational on a fresh machine.

**Current behavior:**
No installer exists. Scripts are not on PATH. `.notes-context/` files do not exist on a fresh clone.

**Desired behavior:**
Running `./install.sh` from the repo root does exactly four things, safely re-runnable:
1. Creates `~/notes/` if it does not exist.
2. Creates each `.notes-context/` file (manifest.md, history_index.md, user-planner.md, daily-goals.md, goals-archive.md) if it does not already exist — never overwrites existing files.
3. Writes `~/.config/fish/conf.d/aide.fish` containing a single `fish_add_path` line pointing at the repo's `scripts/` directory.
4. Prints a short confirmation of what was done.

`README.md` documents: what `aide` is, prerequisites (fish shell, Python 3), install steps (clone, `chmod +x install.sh`, `./install.sh`, open new terminal), and what each `.notes-context/` file is for.

**Key interfaces:**
- Fish PATH config: `fish_add_path /path/to/aide/scripts` — path must be the actual repo location, not hardcoded
- `.notes-context/` initial file content: empty files are fine; `manifest.md` and `history_index.md` may have minimal headers
- Installer must use `$BASH_SOURCE` or equivalent to resolve the repo path dynamically

**Acceptance criteria:**
- [ ] `install.sh` is executable and completes without errors on a clean machine
- [ ] Running it twice does not overwrite existing `.notes-context/` files or duplicate the fish PATH entry
- [ ] After install and opening a new terminal, `note_organizer.sh` is available by name
- [ ] `README.md` covers what aide is, prerequisites, install steps, and the purpose of each `.notes-context/` file

**Out of scope:**
- Support for zsh or bash PATH wiring (fish only for now)
- Installing skills into opencode or Claude Code (manual step, documented in README)
